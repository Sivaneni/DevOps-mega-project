apiVersion: v1
kind: Service
metadata:
  name: {{  .Release.Name    }}-service
  namespace: {{ .Values.namespace.name }}
  labels:
    app: bankapp
spec:
  type: ClusterIP
  selector:
    app: bankapp
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
      #nodePort: 30080
